package org.example.project.screens.tablet.loadAnalize

data class LoadAnalizeState (
    val l: String
) {
    companion object {
        val InitState = LoadAnalizeState(
            ""
        )
    }
}